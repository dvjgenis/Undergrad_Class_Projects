{
  "name": "harmony-forge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "python3 -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "wait-on http://localhost:8000/health -t 30000 -m get && (cd harmony-forge-redesign && npm run dev)",
    "dev:frontend:only": "cd harmony-forge-redesign && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "patch-package": "^8.0.1",
    "wait-on": "^8.0.1"
  }
}
